name: Build and deploy Next.js static site to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build Next.js and export static site
        run: npx next build && npx next export
        env:
          NEXT_TELEMETRY_DISABLED: 1

      - name: Prevent GitHub Pages from running Jekyll
        run: |
          # ensure .nojekyll exists so Pages does not try to run Jekyll
          # out/ is the default export dir for next export
          mkdir -p out
          touch out/.nojekyll

      - name: Deploy to gh-pages branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          publish_dir: ./out
          github_token: ${{ secrets.GITHUB_TOKEN }}
